# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty. As of 2026-07-19 (the day it was created) it contains no source code, build configuration, tests, CI workflows, or documentation. This file reflects that initial state — there are no build, lint, or test commands to document yet, and no architecture to describe.

Known facts:

- **Remote**: https://github.com/huseyinkar37-ops/Claude-tasar-m (public)
- **Default branch**: `main`

## Guidance for Future Claude Code Instances

- This file will be stale as soon as real code lands. Once the project has a package manifest, build tooling, or source layout, regenerate or update this file (e.g. via `/init`) so it documents the actual commands, architecture, and conventions.
- When bootstrapping the project, record the chosen toolchain here as it is established: how to install dependencies, build, run the test suite (including a single test), and lint.
